import { PublicPage } from "@/components/public-page";

const steps = [
  ["Регистрация", "Создайте аккаунт или войдите в существующий кабинет."],
  ["Настройка задачи", "Выберите источник, город, категорию, лимит записей и дополнительные параметры."],
  ["Запуск", "Парсер уходит в очередь и выполняется в фоне, не блокируя страницу."],
  ["Контроль прогресса", "Следите за статусом, количеством обработанных записей, ошибками и временем выполнения."],
  ["Результаты", "Откройте таблицу результатов, примените фильтры и выберите нужные поля."],
  ["Выгрузка", "Скачайте данные в CSV или Excel для дальнейшей работы."],
];

export default function GuidePage() {
  return (
    <PublicPage
      eyebrow="Инструкция"
      title="Как работать с платформой"
      description="Короткий маршрут от запуска первой задачи до выгрузки готовых данных."
    >
      <section className="public-timeline">
        {steps.map(([title, text], index) => (
          <article key={title}>
            <span>{index + 1}</span>
            <div>
              <h2>{title}</h2>
              <p>{text}</p>
            </div>
          </article>
        ))}
      </section>
    </PublicPage>
  );
}
