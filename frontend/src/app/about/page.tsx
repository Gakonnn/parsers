import { PublicCard, PublicPage, PublicSection } from "@/components/public-page";

const values = [
  {
    title: "Миссия",
    text: "Дать бизнесу быстрый доступ к актуальным данным из открытых источников без ручной рутины и хаотичных таблиц.",
  },
  {
    title: "Точность",
    text: "Система структурирует результаты, сохраняет историю запусков и помогает работать с данными через фильтры, экспорт и базу PostgreSQL.",
  },
  {
    title: "Масштабируемость",
    text: "Архитектура рассчитана на добавление новых источников, очереди задач, отдельные worker-контейнеры и управление лимитами.",
  },
];

export default function AboutPage() {
  return (
    <PublicPage
      eyebrow="О платформе"
      title="Больше чем один парсер"
      description="ParseHub объединяет запуск парсеров, хранение результатов, тарифы, выгрузки и администрирование в едином рабочем кабинете."
    >
      <section className="public-card-grid">
        {values.map((item) => (
          <PublicCard key={item.title} title={item.title} text={item.text} />
        ))}
      </section>

      <PublicSection title="Что получает пользователь">
        <p>
          Пользователь запускает задачи по 2GIS, OLX и Krisha.kz, отслеживает прогресс в реальном времени,
          просматривает результаты на сайте и выгружает нужные поля в CSV или Excel.
        </p>
        <p>
          Администратор управляет пользователями, тарифами, лимитами, оплатами и видит состояние задач без ручной
          проверки файлов на сервере.
        </p>
      </PublicSection>
    </PublicPage>
  );
}
